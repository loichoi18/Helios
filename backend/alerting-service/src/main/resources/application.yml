spring:
  application:
    name: helios-alerting-service

server:
  port: 8081

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics

logging:
  level:
    com.helios: INFO

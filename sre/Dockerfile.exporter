FROM python:3.11-slim

WORKDIR /app

COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY . .

# Non-root user
RUN useradd -m sre
USER sre

EXPOSE 9100

CMD ["python", "exporter/prometheus_exporter.py", "--port", "9100"]
